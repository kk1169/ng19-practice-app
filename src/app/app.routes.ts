import { Routes } from '@angular/router';
import { LinkedSignalDemoComponent } from './pages/linked-signal-demo/linked-signal-demo.component';

export const routes: Routes = [
  { path: '', component: LinkedSignalDemoComponent },
];
